<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.db.UserInfo">

	<insert id="join" parameterType="com.smhrd.model.UserInfo">
		INSERT INTO USER_INFO (EMAIL, PW, NICKNAME, JOIN_DATE) VALUES(#{email}, #{pw}, #{nickname}, TRUNC(SYSDATE))
	</insert>
	
	<select id="login" resultType="com.smhrd.model.UserInfo" parameterType="com.smhrd.model.UserInfo">
		SELECT EMAIL, PW, NICKNAME, TO_CHAR(JOIN_DATE, 'YYYY-MM-DD') as joinDate 
		FROM USER_INFO 
		WHERE EMAIL=#{email} AND PW=#{pw}
	</select>
	
	
	<!-- 이메일 중복 체크 -->
	<select id="checkEmailExists" parameterType="String" resultType="int">
		SELECT COUNT(*) 
		FROM USER_INFO 
		WHERE EMAIL = #{email}
	</select>
	
		<update id="update" parameterType="com.smhrd.model.UserInfo">
		UPDATE USER_INFO 
		<set>
			<if test="pw != null and pw != ''">
				PW=#{pw},
			</if>
			<if test="nickname != null and nickname != ''">
				NICKNAME=#{nickname}
			</if>
		</set>
		WHERE 
			EMAIL=#{email}
	</update>
	
	<select id="selectMemberByEmail" parameterType="string" resultType="com.smhrd.model.UserInfo">
        SELECT * FROM USER_INFO WHERE EMAIL = #{email}
    </select>
	
	<delete id="delete" parameterType="string">
    DELETE FROM USER_INFO WHERE EMAIL = #{email}
	</delete>
	
	
</mapper>